interface:
  display_name: "MOM业务数据生成"
  short_description: "基于模板和内置种子生成 MOM 主数据、资源、工艺和生产订单导入包。"
  default_prompt: "请使用 $mom-business-data-generator 根据场景选择合适蓝图，优先复用内置标准场景或现有 seed JSON，生成或校验 MOM 导入模板所需的业务数据包。"
