# MOM模板下拉枚举与填写约束字典

## 用途

本文件用于约束种子数据、Excel 填报数据和后续自动生成脚本，确保填写值命中模板真实下拉枚举，避免导入时报错。

## 特殊规则

- 行政组织顶层公司的父组织编码建议使用 0。
- 业务组织顶层公司的父组织编码建议使用 0。
- 所有引用属性统一填写对象编码，不填写名称。
- 凡是引用关系同时包含版本号时，版本号必须一并填写。
- 当前 skill 主要用于 POC 验证时，默认采用最低安全口径：用户安全等级使用一般，数据密级使用公开。
- 建议单个 Sheet 导入行数控制在 100 行以内，避免模板导入体验变差。
- 最新模板中的 `集成系统`、`集成数据主键`、`集成创建时间`、资质关系和文件关系字段默认允许留空；若用户明确提供真实外部主键或关系映射，再按真实值填写。

## 已验证的模板枚举注意事项

- 行政组织类型只允许：公司、工厂、部门。
- 业务组织-工厂类型只允许：机械加工专业、装配专业。
- 工作中心-类型只允许：产线、设备组、人员组、外委、组织。
- 工作中心-分类只允许：检验、加工。
- 工艺路线-工艺专业只允许：机加、装配、热表、铸造、钣焊、锻造、通用。
- 工艺路线工序/工序库-工序类型只允许：加工、检验、厂际转工、厂内转工、外委。
- 工序专业类型只允许：机械加工专业、装配专业。
- 当前模板中物料与工装工具的计量单位下拉仅提供：个。
- 生产订单-订单类型只允许：标准。
- 生产订单-业务状态只允许：初始、已展开、已释放、已开工、已完工。
- 生产订单-控制状态只允许：正常、暂停、取消。
- 备料清单-是否必须装入只允许：是、否。

## 系统配置_模板.xlsx

### 填写说明

- 填写说明

### Sheet：行政组织

- `行政组织类型`：公司、工厂、部门

### Sheet：业务组织

- `*密级`：公开、内部、秘密、机密
- `工厂组织类型`：公司、工厂、部门、车间、工段、班组
- `工厂类型`：机械加工专业、装配专业

### Sheet：用户

- `*用户安全等级`：一般、重要、核心
- `性别`：男、女

### Sheet：资质等级

- 默认空表，只有用户明确要求“用户资质矩阵”时才填写

### Sheet：资质等级与用户

- 默认空表，只有存在合法 `资质等级编码` 和 `用户` 映射关系时才填写

## 工厂资源_模板.xlsx

### 填写说明

- 填写说明

### Sheet：供应商

- `*密级`：公开、内部、秘密、机密
- `启用`：是、否

### Sheet：设备

- `瓶颈资源`：是、否
- `*密级`：公开、内部、秘密、机密

### Sheet：工装工具

- `*物料分类`：物料、工装工具
- `物料类别`：原材料、辅助材料、零部件、配套件、设备备件
- `*制造类型`：自制件、外购件、自制+外购
- `计量单位`：个
- `特性分类`：重要件、关键件、一般件
- `启用批次标记`：是、否
- `启用序列号标记`：是、否
- `工装类别`：专用工装、通用工具、工装备件
- `一次性工装标记`：是、否
- `单件工装标记`：是、否
- `*密级`：公开、内部、秘密、机密

### Sheet：工作中心

- `*类型`：产线、设备组、人员组、外委、组织
- `*分类`：检验、加工
- `*密级`：公开、内部、秘密、机密

### Sheet：库房

- `*作业模式`：普通库房、立体库
- `*业务类型`：ERP一级库、ERP二级库、车间二级库、设备备件库、工装备件库
- `*密级`：公开、内部、秘密、机密

### Sheet：库位

- `*密级`：公开、内部、秘密、机密

### Sheet：工序库

- `工序专业类型`：机械加工专业、装配专业
- `*工序类型`：加工、检验、厂际转工、厂内转工、外委
- `执行标记`：是、否
- `*时间单位`：天、小时、分钟、秒
- `*密级`：公开、内部、秘密、机密

### Sheet：工序库与资质等级

- 默认空表，只有存在合法 `工序库编码` 与 `资质等级编码` 映射时才填写

## 产品与工艺_模板.xlsx

### 填写说明

- 填写说明

### Sheet：物料

- `*物料分类`：物料、工装工具
- `物料类别`：原材料、辅助材料、零部件、配套件、设备备件
- `*制造类型`：自制件、外购件、自制+外购
- `计量单位`：个
- `特性分类`：重要件、关键件、一般件
- `启用批次标记`：是、否
- `启用序列号标记`：是、否
- `工装类别`：专用工装、通用工具、工装备件
- `一次性工装标记`：是、否
- `单件工装标记`：是、否
- `*密级`：公开、内部、秘密、机密

### Sheet：物料与文件关系

- 默认空表
- `*物料版本号`、`*物料编码`：引用已存在于当前模板中的物料
- `*文档实体类型`、`*文档对象ID`：仅在用户明确提供目标系统合法文档对象时填写

### Sheet：MBOM

- `*密级`：公开、内部、秘密、机密

### Sheet：MBOM节点

- `*物料类别`：原材料、辅助材料、零部件、配套件、设备备件
- `制造类型`：自制件、外购件、自制+外购
- `计量单位`：个
- `*MBom版本号`、`*MBom编码`：注意字段名大小写按最新模板口径填写

### Sheet：工艺路线

- `*工艺类型`：正式工艺、典型工艺、临时工艺、一级工艺、返工工艺、返修工艺
- `工艺专业`：机加、装配、热表、铸造、钣焊、锻造、通用
- `*密级`：公开、内部、秘密、机密

### Sheet：工艺路线工序

- `*工序类型`：加工、检验、厂际转工、厂内转工、外委
- `*时间单位`：天、小时、分钟、秒
- `执行标记`：是、否
- `工序专业类型`：机械加工专业、装配专业

### Sheet：工艺路线工序序列

- `*接续关系`：ES、SSEE、无

### Sheet：工艺路线与资质等级关系

- 默认空表，只有存在合法 `资质等级编码` 且明确要求工艺资格控制时才填写

## 生产订单_模板.xlsx

### 填写说明

- 填写说明

### Sheet：生产订单

- `订单类型`：标准
- `*计量单位`：个
- `计划类型`：无、零部件交付计划、零部件加工计划
- `排产状态`：无、零部件交付计划已排产
- `业务状态`：初始、已展开、已释放、已开工、已完工
- `*控制状态`：正常、暂停、取消
- `释放状态`：未释放、部分释放、全部释放
- `*密级`：公开、内部、秘密、机密

### Sheet：备料清单

- `是否必须装入`：是、否
